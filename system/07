/usr/sbin/service ssh status | grep "Main" | awk ' {print $3} '
# OR
# cat /var/run/sshd.pid
# A PID (i.e., process identification number) is an identification
# number that is automatically assigned to each process 
